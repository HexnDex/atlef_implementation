# ATLEF: Verified-Compatible Environment
#
# IMPORTANT: this is NOT a historical record of the exact environment
# that produced the results reported in the thesis. That environment's
# Colab runtime disconnected at some point after the original runs and
# could not be recovered; pip does not retain version history once a
# runtime is gone.
#
# This file instead records a environment captured from a fresh Colab
# session in September 2026, confirmed to satisfy the package names
# and pinned versions the notebook explicitly requests. Two known
# differences from the original runs:
#
#   1. CUDA build: the original notebook installs from the cu118
#      (CUDA 11.8) index; this environment resolved to cu128
#      (CUDA 12.8), Colab's current default. Both are expected to
#      produce numerically equivalent results for this pipeline, but
#      this was not re-verified against the original outputs.
#   2. reedsolo, piq, and pyiqa (installed later in the notebook, not
#      part of the initial setup cell) were not present in this
#      session and are not pinned below; requirements.txt's floor
#      versions are the best available reference for these three.
#
# Where the notebook explicitly pins a version (pytorch-lightning,
# accelerate), this environment matched it exactly.

torch==2.11.0+cu128
torchvision==0.26.0+cu128
torchaudio==2.11.0+cu128
numpy==2.1.3
pandas==2.2.3
pillow==11.3.0
opencv-python==5.0.0.93
scikit-image==0.25.2
pytorch-lightning==1.9.5
accelerate==0.21.0
einops==0.8.2
omegaconf==2.3.1
ftfy==6.3.1
regex==2025.11.3
tqdm==4.67.3
scipy==1.16.3
matplotlib==3.10.0
safetensors==0.8.0

# Not present in this session; version floors only (see requirements.txt):
# reedsolo>=1.5
# piq>=0.8
# pyiqa==0.1.16
